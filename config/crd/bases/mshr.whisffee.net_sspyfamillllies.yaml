---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: sspyfamillllies.mshr.whisffee.net
spec:
  group: mshr.whisffee.net
  names:
    kind: SSpyFamilllly
    listKind: SSpyFamillllyList
    plural: sspyfamillllies
    singular: sspyfamilllly
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.replicas
      name: REPLICAS
      type: integer
    - jsonPath: .status
      name: STATUS
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: SSpyFamilllly is the Schema for the sspyfamillllies API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: SSpyFamillllySpec defines the desired state of SSpyFamilllly
            properties:
              dbHost:
                type: string
              dbPassword:
                type: string
              dbPort:
                type: integer
              dbUser:
                type: string
              sourceDatabase:
                type: string
              sourceTable:
                type: string
              targetDatabase:
                type: string
            type: object
          status:
            description: SSpyFamillllyStatus defines the observed state of SSpyFamilllly
            enum:
            - NotReady
            - Available
            - Healthy
            type: string
        type: object
    served: true
    storage: true
    subresources:
      status: {}
